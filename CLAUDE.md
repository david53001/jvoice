# CLAUDE.md — JVoice

Standalone macOS menu-bar voice-dictation app. Press ⌥Space anywhere → record → on-device WhisperKit transcription → tone-styled text pasted into the frontmost app. Free, open-source, privacy-first: zero network calls at runtime (only the one-time Whisper model download), no telemetry, no accounts.

## Provenance

Extracted 2026-06-06 from `../MacOSUtils` (David's multi-utility app, formerly named JVoice). **Never modify `../MacOSUtils`** — it is a separate, active project. This repo is the public-facing standalone product.

## Hard rules

- **NO publishing without David's explicit go-ahead.** Do not `gh repo create`, `git push`, or add remotes. He has two gh accounts (`david53001` active, `da97d`); publishing is deliberately on hold. Same applies to posting anywhere (HN/Reddit drafts exist in `docs/launch/` — David posts those himself).
- **$0 budget.** No Apple Developer account → unsigned distribution (ad-hoc signed DMG via GitHub Releases). Homebrew is NOT an option (bans unsigned casks as of Sept 2026 — see `docs/launch/unsigned-distribution-findings.md`). Everything must stay free: GitHub Pages for the site, free CI, no paid services.
- The app stays **free forever**; potential future model is VoiceInk-style (free buildable source + paid prebuilt binary) — but that's a David decision, not yours.

## Build & test

- `swift build` — must pass. SwiftPM, Swift 5.9, macOS 14+. Deps: WhisperKit (≥1.0.0), KeyboardShortcuts (exact 1.10.0). `Package.resolved` is tracked deliberately (reproducible builds).
- `swift test` — **compiles but executes 0 tests locally**: this machine has Command Line Tools only (no swift-testing runner). Real test runs happen in CI (`.github/workflows/test.yml`, macos-15 + Xcode 16, which asserts ≥90 swift-testing cases actually executed — older Xcode silently compiles the suite out via `#if canImport(Testing)`).
- `./scripts/run-logic-tests.sh` — REAL local verification for the pure-logic sources (TextProcessor, PhoneticMatcher, VocabularyPrompt, RepetitionGuard incl. a 120-case loop fuzz, WavTail, ChunkPlanner): compiles them with a standalone assertion main and runs it.
- `./scripts/verify-streaming.sh` — REAL local verification (compiles + EXECUTES, no WhisperKit/mic) of the streaming session's data-loss guarantee (empty non-silent chunk → fallback, never a silent drop) and the regurgitation-recovery policy, using mock decoders.
- `python3 scripts/verify-transcription.py --model tiny|base|small|large [--quick]` — end-to-end harness: generates many `say` clips (varied length/pauses/voices, custom words spoken or not) and scores word-retention (no dropped speech) + spurious-vocab (no hallucinated custom words) across whole-file AND streaming. Needs the model downloaded.
- `.build/release/JVoice --bench <wav> [--model tiny|base|small|large] [--vocab "A,B"] [--stream] [--lang en|ro] [--no-prompt]` — hidden CLI mode for measuring transcription speed and verifying vocabulary biasing / streaming end-to-end on this machine (generate clips with `say` + `afconvert`). `--stream` simulates a live recording at 10×; `--no-prompt` A/B-tests the decoder prompt.
- `swift scripts/generate-icon.swift` — regenerates `Resources/AppIcon.icns` (black-squircle "J") and `docs/demo-video/public/app-icon.png`.
- `./scripts/install.sh` — release build, signs with the local "JVoice Self-Signed" keychain cert, installs to /Applications. Only run when David asks to install/dogfood.
- Bundle ID is `com.jvoice.app` — deliberately NOT `com.jvoice.JVoice` (that's MacOSUtils'; both apps coexist on this machine). UserDefaults namespace: `jvoice.app.*`.

## Architecture (Sources/JVoice/)

- `VoiceCoordinator.swift` — central orchestrator: hotkey → record → transcribe → process → paste flow, HUD state (menu bar mirrors it), stats.
- `Services/` — RecordingManager (AVAudioRecorder, orphan-WAV sweep), TranscriptionManager (WhisperKit engine; WhisperModelLocator). **Custom-word accuracy + robustness is layered**: VocabularyPrompt builds the decoder-conditioning `promptTokens` (the main accuracy lever — gets "Li-Fraumeni"/"VS Code" right; kept ON by default); RepetitionGuard detects/strips "prompt regurgitation" (the decoder reciting the vocab list on pauses/silence) and flags it via `scrub`; RegurgitationRecovery re-decodes the same audio *without* the prompt **only when** a decode regurgitated or came back empty — keeping prompt accuracy in the common case while making the failure mode (loops, scattered insertions, dropped speech) unreachable. The engine also duration-gates `withoutTimestamps` (long clips MUST keep timestamps — WhisperKit 1.0.0 truncates otherwise) and runs `TextProcessor.stripDecoderArtifacts` (drops `[BLANK_AUDIO]`-style sentinels). **Streaming-while-recording**: WavTail (growing-WAV parser), ChunkPlanner (pure silence-cut chunk policy), StreamingTranscriptionSession (decodes completed chunks during recording; any failure or an empty non-silent chunk → whole-file fallback, *never* a silent drop). **Post-processing**: TextProcessor (tone styles, filler removal, exact custom-word corrections, hallucination-sentinel stripping), PhoneticMatcher (fuzzy sound-alike correction, "jay voice"→"JVoice"). Plus PasteManager (Accessibility paste), HotKeyManager, AudioInputRouter (keeps Bluetooth on A2DP by recording from built-in mic), SettingsStore, StatsStore, LastTranscriptStore, BenchRunner (hidden `--bench` CLI).
- `Models/` — AppMode (tone styles), HUDState (incl. `.preparingModel` for first-load waits), WhisperModelOption, TranscriptionLanguage, SettingsState.
- `UI/` — HUDView/HUDWindow (recording/preparing/transcribing/done pills), SettingsView/SettingsWindow, MenuBarController (status item: bold "J" template image idle, red mic recording, cyan waveform transcribing + NSMenu).

## Demo video (Remotion)

`docs/demo-video/` is a Remotion project that renders the README demo as a faithful recreation of the real UI. **All visuals must trace to `docs/demo-video/DESIGN-TOKENS.md`**, which was extracted from the SwiftUI source — if app UI changes, regenerate tokens first. Storyboard and fidelity were approved by David (rejected an earlier generic mockup; product-exact UI is the acceptance bar).
- Render: `cd docs/demo-video && npm install && npx remotion render` (see package.json scripts).
- Outputs: `docs/assets/demo.mp4` (1600×1000@30fps) and `docs/assets/demo.gif` (800px@20fps, keep ≤8MB).
- After re-rendering, copy the gif to `../Portfolio/assets/jvoice-demo.gif` (the download site, its own git repo).
- Review stills live in `docs/demo-video/stills/`.

## Launch material

- `docs/launch/distribution-plan.md` — researched channel playbook (r/macapps → Show HN → awesome-lists → directories; TikTok for non-tech users).
- `docs/launch/unsigned-distribution-findings.md` — Gatekeeper "Open Anyway" reality, DMG-not-zip rule, Homebrew ban, VoiceInk model.
- `docs/launch/launch-post-drafts.md` — HN/Reddit/press drafts. David posts these personally (HN bans AI-written replies).
- `README.md` — conversion-focused; `USER` is a literal placeholder for the GitHub username, replace at publish time (also in `../Portfolio` and the launch drafts).

## Current status & next steps

See `docs/HANDOFF.md` for session-by-session state, open questions, and the agreed next actions.

## Windows port (in progress — `windows/`)

A native **Windows** port of JVoice lives under `windows/` (a separate .NET solution). The macOS Swift app above is **unchanged and read-only** — it is the reference and the source-of-truth for the accuracy "brain" and its invariants. **Do not modify `Sources/`, `Tests/`, `Package.swift`, or `Resources/` when working on the Windows port.**

- **Stack:** C# on **.NET 9**, **WPF** (`win-x64`, WinExe), speech via **Whisper.net** (managed whisper.cpp bindings, GGML models) with **CUDA** GPU acceleration + a CPU fallback. This replaces macOS-only WhisperKit/CoreML. NAudio for capture, H.NotifyIcon.Wpf for the tray, SkiaSharp for the icon. The dev machine has an RTX 3060 Ti (CUDA) + i5-12400 (AVX2).
- **The brain ports faithfully.** `JVoice.Core` reproduces TextProcessor / PhoneticMatcher / VocabularyPrompt / RepetitionGuard / RegurgitationRecovery / ChunkPlanner / WavTail / StreamingTranscriptionSession 1:1 (every constant verbatim), locked by `JVoice.Tests` (xUnit) translated from the Swift tests. The WhisperKit-specific workarounds (SuppressBlankFilter, single-window timestamp trap) are dropped — whisper.cpp doesn't need them. Two **Windows-only** Core additions have no macOS equivalent: `Audio/HighPassSilence` (now metrics-only) and `Text/NonSpeechAnnotation` (the model-driven no-speech detector that replaced the RMS gate — §7 #21).
- **Plan:** the complete, zero-context, phase-by-phase implementation plan is in `docs/superpowers/plans/2026-06-22-windows-port-0{0..5}-*.md`. Read `…-00-overview.md` first (architecture, canonical names, constraints, rejected alternatives, cross-phase reconciliation §10). Phases: 1 Core brain → 2 Whisper engine → 3 platform (audio/hotkey/paste/persistence) → 4 WPF UI + VoiceCoordinator → 5 packaging/CI/docs.
- **Build/test/run:** `dotnet build windows/JVoice.sln -c Release` · `dotnet test windows/JVoice.Tests` · `dotnet run --project windows/JVoice.App` · publish single-file per Phase 5.
- **Codebase layout (2026-06-26 reorg):** `windows/` is organized into `@`-mentionable area folders, each with its own `CLAUDE.md` brief — start at `windows/CLAUDE.md` (the area index). `JVoice.App/Platform/` → `Capture/` + `Persistence/` + `System/`; the `JVoice.Core` root decision helpers → `JVoice.Core/Policy/`. Pure `git mv` (namespaces are declared in-file and were left unchanged, so folder ≠ namespace is intentional — don't realign as a drive-by).
- **⚠ Known bug — do NOT run elevated:** launching JVoice elevated ("Restart as Administrator") freezes the first recording (HUD stalls, the stop press never registers, nothing pastes); **run non-elevated**. The §7 #25 elevated-paste path is broken until root-caused — see `docs/HANDOFF-WINDOWS.md` §8.
- **Same constraints as the macOS side:** $0 budget (no paid code-signing → unsigned `.exe`; document the SmartScreen "More info → Run anyway" step, the Windows analog of Gatekeeper "Open Anyway"); GPL-3.0 (all NuGet deps MIT-compatible); privacy (zero runtime network except the one-time GGML model download); **NO publishing/pushing without David's go-ahead.** Default hotkey is **Ctrl+Shift+Space** (⌥Space has no clean Windows equivalent — Alt+Space is the system window menu).
- **Status:** all 5 phases implemented. `dotnet build windows/JVoice.sln -c Release` = 0 errors; `dotnet test` = 650/650; on-device transcription verified (whisper-smoke / `--bench`, Vulkan GPU + CPU); **the GUI launches to the tray** (two startup crashes found & fixed: `TaskbarIcon.ForceCreate` efficiency-mode COMException, and the PNG→`System.Drawing.Icon` conversion). **UI is a black-&-white redesign (2026-06-23):** the HUD is a text-free black pill of white **voice-activity bars** — a continuous, mic-independent wave while recording (David preferred a steady flow over mic-reactive bars, which stuttered on his words; indeterminate shimmer while transcribing); a successful paste is **silent** (no "Pasted" pill); errors are the only text; Settings + tray are fully monochrome. The bars also fix the HUD blur David saw at his non-native 1600×1080 gaming res (solid shapes beat the old layered-window text AA; `DisplayMetrics.HudScale` still enlarges by the stretch ratio — fix is IN-APP, never change his resolution). **No-speech is model-driven (2026-06-23):** the RMS/spectral pre-gate was **RETIRED** — whisper itself decides, so David's quiet/short dictation transcribes while true silence/hum/noise → "No speech detected." (`JVoice.Core/Text/NonSpeechAnnotation` maps whisper's `[BLANK_AUDIO]`/`(birds chirping)` annotations to empty); the streaming path also no longer drops a quiet sentence **tail**. **Do NOT add digital gain to the *transcription* audio to "fix accuracy"** — whisper normalizes log-mel energy and decodes his quiet audio correctly; gain scales noise equally (no SNR win) and risks clipping. The only real accuracy levers are mic SNR + diction (the HUD bars are a generated wave, not a mic meter — §7 #23). Screenshot any state via `JVoice.exe --hud-preview <state>` / `--settings-render <png>` / `--hud-render <png>`. Remaining: David's interactive **dogfood** of the live dictation loop + HUD/Settings visuals (`docs/launch/windows-dogfood-checklist.md`); optional Inno installer + verify-transcription harness. Full as-built state, pinned versions, and all deviations/gotchas are in `docs/HANDOFF-WINDOWS.md` (the zero-context anchor) — see §7 #18 (HUD redesign), #21 (no-speech retired), #22 (the no-transcription-gain guard), #23 (HUD bars → continuous wave + final pill geometry) #25 (opt-in "run elevated" so the hotkey works in admin windows — UIPI; plus the hook now swallows the chord key so no stray space leaks), and #26 (Settings reordered to mirror macOS + a Windows-only **Recent Transcripts** history — last 30, hover-reveal Copy/Delete + Clear all, persisted to `transcript-history.json`, cleared by Restore Defaults; the old editable Last Transcript box was removed), and #27 (**game-detection hotkey suppression** — Windows-only, David-requested 2026-06-25: the hotkey goes **silent and passes the chord straight through to the game** when a video game owns the foreground, so an accidental Ctrl+Shift+Space in Minecraft/GTA/Fortnite/Valorant can't pop the HUD or paste into game chat. **Anti-cheat-safe by construction** — read-only OS signals only: `SHQueryUserNotificationState` D3D-fullscreen, Windows `GameConfigStore`, install-path/exe-name match, window-rect-vs-monitor; the ONLY process access is `PROCESS_QUERY_LIMITED_INFORMATION` to read the exe path — **no memory reads, no module enumeration, no injection** (the graphics-DLL module-scan signal was deliberately dropped to keep process interaction at zero). Off/Balanced/Aggressive `GameMode` (`SettingsState` schema v2); **Balanced** (default) excludes bare fullscreen so fullscreen video never false-positives. `JVoice.exe --game-probe` prints live signals to `%TEMP%\jvoice-gameprobe.log` for dogfood), and #28 (opt-out **developer-terms** correction pack — recognizes coding vocab like "node js"→Node.js / "jason"→JSON via the post-processing channel, NOT the decoder prompt; default ON; built on branch `feat/developer-terms`), and #29 (**Settings widened to a two-column layout**, David-requested 2026-06-26: `SettingsView.xaml` went from a tall scrolling `320×520` single column to a wider `640×846` two-column "masonry" — full-width header + two independent vertical-StackPanel columns + full-width footer, all in the outer `ScrollViewer` with `HorizontalScrollBarVisibility="Disabled"` so the `*` columns resolve; shows everything at once with no scrolling for a typical state. Same change fixes two layout bugs structurally: the Developer Terms subtitle no longer slides under its toggle (2-col Grid: `*` text + wrap, `Auto` switch) and the Corrections/Custom-Words "Add" buttons are never clipped (flex `*` inputs + `Auto` button). Four `x:Name`s preserved → no code-behind change; the `--settings-render` harness now follows the view's declared size), and #30 (**installed app + both `~/Downloads` installers refreshed to the §29 two-column build `6f8ffc2`**, 2026-06-26: the autostart `HKCU\…\Run\JVoice` was repointed from the stale dev `bin\x64\Release` exe to `"<install>\JVoice.exe" --autostart`; both one-click installers rebuilt via the IExpress `.sed` flow; **distribution decision — CPU `JVoice-Setup.exe` is the default for everyone, GPU `JVoice-Setup-GPU.exe` is optional/NVIDIA-only** (`docs/launch/windows-distribution.md` + `windows-release-notes-draft.md`); refresh the install with `robocopy /MIR` from a fresh `JVoiceFlavor=gpu` publish — NOT `Remove-Item -Recurse` (sandbox-blocked) — and quit any **elevated** instance via the tray first since it locks the install dir; NOT published, repo stays private), and #31 (**Whisper transcription speed-up, 2026-06-27**: a measured tuning layer on the existing Whisper.net 1.9.1 engine — **flash attention ON for GPU builds** (Vulkan large-v3-turbo ≈30–37% faster, transcripts identical; forced OFF in the `cpu` flavor via `JVOICE_CPU` since flash degrades CPU decode) + **`WithThreads` = physical core count** (CPU-build ≈21% faster). Per-clip `audio_ctx` measured non-monotonic → NOT adopted; CUDA confirmed unavailable without the toolkit → opt-in only. New pure `JVoice.Core/Policy/WhisperTuning.cs` + `JVoice.App/Whisper/EngineTuning.cs` + `Platform/System/CpuInfo.cs`; brain untouched; `--bench` instrumented with `--flash`/`--threads`/`--audio-ctx`/`--runtime`. `dotnet test` 555/555), and #32 (**dictation-features batch — quick-wins + app-aware modes, 2026-07-01, branch `feat/dictation-modes`, David-requested**: schema **v2→v3** adds five Windows-only settings, each with safe defaults + per-field JSON fallback. Four quick wins — **clipboard-only** paste (`Paster.Stage`; "Copy to Clipboard" toggle), a **time-saved** stat (pure `StatsMath.EstimatedMinutesSaved`, 40-wpm baseline; the Stats card is now 3-up), **dictate-to-translate** (Whisper.net 1.9.1 **does** expose `WithTranslate()` — confirmed by reflecting the DLL; engine ctor gained a trailing `translate` flag; "Translate to English" toggle rebuilds the engine), and an **opt-in undo-last-paste** SECOND global hotkey (**no default** — a registered global chord is swallowed system-wide; `Paster.SendUndo`→Ctrl+Z; one-shot, game-suppressed) — plus **app-aware modes**: new Windows-only `ToneStyle.Code` (minimal formatting; out of the manual `Toggled` cycle) + pure `AppModeResolver` (user rules beat a built-in terminals/editors/IDEs→Code list) applied per foreground app via read-only `ForegroundApp.ExeName` (**same anti-cheat-safe access class as GameDetector** — `PROCESS_QUERY_LIMITED_INFORMATION` only). **"switch models" in the ask = modes (tone), NOT Whisper models** — per-app model override left as a future field. Settings grew to `640×1080` with a new "App Modes" card (click-to-cycle mode chip, not a ComboBox). Brain untouched; `dotnet test` **608/608**; NOT pushed — see `docs/HANDOFF-WINDOWS.md` §7 #32), and #33 (**Settings two-column → three-column so the window fits the screen, David-requested 2026-07-01**: the §32 additions pushed the two-column `640×1080` panel so tall that at David's non-native **1600×1080** desktop the `SizeToContent`+`CenterScreen` window ran off the top of the screen — he **couldn't click the title-bar X**. `SettingsView.xaml` went **640→960 wide, three-column masonry** (11 cards redistributed; the four list cards kept in separate columns), each column still ~300px so no card is more cramped; the view's fixed `Height` was removed so it **sizes to content → 960×757**, rendered-verified. Two guards against recurrence: `SettingsWindow` clamps `MaxHeight = WorkArea.Height − 16` and the outer `ScrollViewer` scrolls on overflow. `--settings-render` now does **two** measure passes (the outer ScrollViewer's extent settles only after the first layout pass — one pass under-measured and clipped). Code-behind unchanged; `dotnet test` **608/608**; NOT pushed — see `docs/HANDOFF-WINDOWS.md` §7 #33), and #34 (**developer-terms pack — AI / "vibe coding" expansion, David-requested 2026-07-01, branch `feat/dictation-modes`**: added a `// ---- AI / vibe coding ----` block to `JVoice.Core/Text/DeveloperTerms.cs` — **+58 heard-form keys** (Map `107 → 165`): tools/agents (Copilot, Claude Code, Windsurf, Ollama, Replit…), frameworks/protocols (MCP, LangGraph, LlamaIndex, CrewAI, vLLM…), models/labs (GPT, DeepSeek, Qwen, Gemini, Mistral, Groq), vector DBs (Weaviate, ChromaDB, Qdrant, pgvector, FAISS…), and the modern deploy stack (Vercel, Netlify, Supabase, Firebase, Cloudflare, tRPC, SvelteKit, Deno, Zod…). **No code/API/schema/wiring change** — flows through the existing `DeveloperTerms.Augment` call, still gated by the Developer Terms toggle, still default ON; brain otherwise untouched. The real work was the exclusion audit against the `\bword\b` matcher: product names that are also everyday English are **EXCLUDED and test-locked** — most importantly **`cursor`** (the text/mouse cursor!), plus `bolt`/`continue`/`render`/`railway`/`remix`/`warp`/`astro`/bare `svelte`/`bun`/**`pinecone`**/bare `chroma`/`cohere`/**`perplexity`** (also an ML metric)/`grok` (the verb; only `groq` kept)/`drizzle`/`lovable`/bare `llama`; their products stay reachable via the user's own custom-words/correction rules. Three kept homophones mirror `jason`→JSON: `groq`→Groq, `gemini`→Gemini (safe either way), and distinctive `mistral`/`firebase`/`windsurf`. `dotnet test` **650/650** (DeveloperTermsTests 30→72); NOT pushed — see `docs/HANDOFF-WINDOWS.md` §7 #34), and #35 (**Whisper model now defaults to Large + a "keep on Large" advisory, David-requested 2026-07-01, branch `feat/dictation-modes`**: `SettingsState.Default.Model` and the `SettingsStateJson.ParseModel` fallback both changed `Tiny → LargeTurbo`, kept in sync so *any* unspecified-model path lands on Large — a deliberate Windows-only divergence from macOS's `.tiny`, since on David's GPU large-v3-turbo is the most accurate **and** fast (§7 #31). **No schema change** (still v3); a fresh user just downloads the ~574 MB GGML on first use via the existing model-store flow. The Settings "Whisper Model" card gained a monochrome warning callout (Segoe MDL2 `E7BA` triangle) — **"Keep this on Large."** always shown, plus an extra caution line bound to `IsLarge` via `InverseBoolToVis` that appears only when a smaller model is picked (no code-behind / VoiceCoordinator change). 3 test assertions updated; `dotnet test` **650/650**; both UI states rendered via `--settings-render`; committed `fcbbe50`, NOT pushed — see `docs/HANDOFF-WINDOWS.md` §7 #35).
